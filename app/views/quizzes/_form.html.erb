<%= form_for @quiz do |f| %>
  <%= render partial: 'application/error', locals: {object: @quiz, txt: 'quiz'} %> 

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.hidden_field :host_id %>

  <%= f.submit %>
<% end %>

<%# USED BY quizzes/new and quizzes/edit %>