<h1><%= @quiz.title %> </h1>
Rounds (<%= @quiz.rounds.count %>):
<% @quiz.rounds.each do |round| %>
  <li>
    <%= round.subject %>
  </li>
<% end %>
<p>
  <%= link_to 'Delete Quiz', quiz_path(@quiz), method: :delete %>
</p>