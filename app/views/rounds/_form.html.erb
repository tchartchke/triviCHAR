<%= form_for @round do |f| %>
  <%= render partial: 'application/error', locals: {object: @round, txt: 'round'} %> 

  <%= f.label :subject %>
  <%= f.text_field :subject %>

  <%= f.hidden_field :quiz_id %>

  <%= f.submit sub_text%>
<% end %>

<%# Used in quizzes/show and rounds/edit %>